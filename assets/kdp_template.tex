\documentclass[11pt,twoside,openright]{book}
\usepackage{fontspec}
\usepackage{polyglossia}
\setmainlanguage{english}
\setmainfont{DejaVu Serif}
\usepackage[paperwidth=$TRIM_WIDTH$in,paperheight=$TRIM_HEIGHT$in,
  inner=$GUTTER$in,outer=$OUTER$in,top=$TOPBOTTOM$in,bottom=$TOPBOTTOM$in]{geometry}
\usepackage{titling}
\usepackage[hidelinks]{hyperref}
\usepackage{enumitem}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}

% Body text: indented paragraphs, no space between, fully justified (LaTeX default)
\setlength{\parindent}{1.4em}
\setlength{\parskip}{0pt}

% Prevent overfull boxes by letting LaTeX stretch word spacing instead of
% overflowing the margin when a line is hard to break.
\tolerance=9999
\emergencystretch=3em
\hbadness=10000

\setcounter{tocdepth}{0}
\setcounter{secnumdepth}{0}

% Add dot leaders to chapter-level TOC entries
\makeatletter
\renewcommand{\l@chapter}[2]{%
  \ifnum \c@tocdepth >\m@ne
    \addpenalty{-\@highpenalty}%
    \vskip 1.0em \@plus\p@
    \setlength\@tempdima{1.8em}%
    \begingroup
      \parindent \z@ \rightskip \@tocrmarg
      \parfillskip -\rightskip
      \leavevmode\bfseries
      \advance\leftskip\@tempdima
      \hskip -\leftskip
      #1\nobreak\leaders\hbox{$\m@th\mkern \@dotsep mu\hbox{.}\mkern \@dotsep mu$}\hfill
      \nobreak\hb@xt@\@pnumwidth{\hss #2}%
      \par
    \endgroup
  \fi}
\makeatother

\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\bfseries}{\Large\thechapter}{6pt}{\Large}
\titlespacing*{\chapter}{0pt}{0pt}{20pt}
% unnumbered chapters (front/back matter) use same display style
\titleformat{name=\chapter,numberless}[display]
  {\normalfont\bfseries}{}{0pt}{\Large}

\usepackage{fancyhdr}
\setlength{\headheight}{14pt}
\pagestyle{fancy}

% Give blockquotes (Evidence Checkpoint boxes) extra safety margin on both sides
\usepackage{quoting}
\renewenvironment{quote}{\list{}{\leftmargin1.2em\rightmargin1.2em}\item[]}{\endlist}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\fancyhead[LE]{\nouppercase{\thepage}\quad\textit{$author$}}
\fancyhead[RO]{\textit{$title$}\quad\nouppercase{\thepage}}
\fancypagestyle{plain}{%
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
}

\title{$title$}
\author{$author$}

\begin{document}
\pagestyle{empty}

\input{00a_front_matter_pages.tex}

% ---- Front matter: TOC, Acknowledgments, Prologue (roman numerals, unnumbered) ----
\pagenumbering{roman}
\setcounter{page}{1}

\tableofcontents
\cleardoublepage
\pagestyle{fancy}

$frontmatter$

% ---- Main matter: numbered chapters, arabic numerals restart at 1 ----
\cleardoublepage
\pagenumbering{arabic}
\setcounter{page}{1}

$body$

% ---- Back matter: unnumbered chapters, arabic numbering continues ----
\cleardoublepage
$backmatter$

\end{document}
